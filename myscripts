-- Multiplier Test Script (3x Damage)
-- Target: SwordDamage RemoteEvent

local TargetRemoteName = "SwordDamage"
local NewMultiplier = 3

-- Use the hookmetamethod provided in your environment dump
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}

    -- Check if the calling method is FireServer and the object is the SwordDamage remote
    -- We use checkcaller() to ensure we don't hook our own potential scripts
    if not checkcaller() and method == "FireServer" and self.Name == TargetRemoteName then
        
        -- Based on the decompiled Client script:
        -- args[1] = Humanoid (Target)
        -- args[2] = Tool (Sword)
        -- args[3] = Multiplier (This is what we want to test)
        -- args[4] = Combo count
        
        -- Override the multiplier to 3
        args[3] = NewMultiplier
        
        -- Optional: Force the combo to 10 (the max in the script) for extra testing
        args[4] = 10 

        -- Return the original function with our modified arguments
        return oldNamecall(self, unpack(args))
    end

    return oldNamecall(self, ...)
end)

print("Damage Multiplier Test Active: Forced to " .. tostring(NewMultiplier) .. "x")
